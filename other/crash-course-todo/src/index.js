import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//We are rendering the App component
//into the document element "root" (on index.html)
ReactDOM.render(<App />, document.getElementById('root'));
